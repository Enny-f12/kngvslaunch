// postcss.config.mjs

const config = {
  plugins: {
    // Correct structure: key is the plugin name, value is an options object (empty in this case)
    '@tailwindcss/postcss': {}, 
  }
};

export default config;